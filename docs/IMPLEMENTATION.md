# Implementation notes

## Current scope

This implementation covers GitHub issues 2–5:

1. the `FruitReflection` Prisma model and initial PostgreSQL migration;
2. the daily reflection form;
3. today’s dashboard status;
4. recent reflection history and detail pages.

These issues form one vertical slice: a person can write, persist, revisit, and
recognise completion of a reflection before trend or highlight features are
built.

## Structure

- `app/` contains routes and Server Actions.
- `components/` contains reusable form and display components.
- `lib/` contains date, fruit, database, and query concerns.
- `prisma/` contains the schema and immutable migrations.
- `generated/prisma/` is generated by Prisma and is not hand-edited.

Database reads stay in Server Components. Mutations use Server Actions with Zod
validation and accessible progressive-enhancement states.

## Data model

`FruitReflection.reflectionDate` is a PostgreSQL `DATE` with a unique constraint.
This enforces the MVP rule of one reflection per day. Fruits are a PostgreSQL
enum array so invalid fruit values cannot enter the database.

The MVP is deliberately single-user because authentication and account ownership
are not part of the current issue set. Before adding authentication, add a user
relation and change the unique constraint to `(userId, reflectionDate)`.

## Dates and timezones

`APP_TIME_ZONE` determines the current calendar day. Local development uses
`Europe/London` in the provided example. This is an application-level bridge
until a future user profile stores each person’s timezone.

Reflection dates are formatted as UTC because the database value is a calendar
date, not an instant. This avoids displaying the previous day in negative UTC
offsets.

## Deployment

Prisma uses the PostgreSQL driver adapter, which works with a standard local
PostgreSQL URL and Neon’s pooled connection URL. Vercel must receive:

- `DATABASE_URL`: the pooled Neon PostgreSQL URL;
- `APP_TIME_ZONE`: the timezone used for “today”.

Use `prisma migrate deploy` for production migrations. Pages call Next.js
`connection()` before database reads, keeping database work at request time
instead of during Vercel’s build.

## Product and usability guardrails

- Language describes awareness and growth, never performance or holiness scores.
- No streaks, rewards, or gamified completion mechanics.
- The form works without client-side navigation and reports server validation
  errors in an `aria-live` region.
- Color is not the only selected-state indicator; fruit choices also use borders
  and native checked state.
- Motion respects `prefers-reduced-motion`.
